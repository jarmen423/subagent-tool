Implementation is complete. Here's what was built:

Summary
Package: cursor-subagent at d:\code\automations

Architecture
cursor-subagentd — FastAPI daemon on 127.0.0.1:17340 holding in-memory SDK agent handles for stateful multi-turn sessions
SQLite at ~/.cursor/subagents/subagents.db — sessions, runs, events, waves
cursor-subagent CLI — thin HTTP/WebSocket client; auto-starts daemon on first use
AgentProvider protocol — v1 backend cursor-composer via cursor-sdk; ready for future providers
Commands
cursor-subagent daemon start|stop|status
cursor-subagent spawn --task "..." --cwd . --json
cursor-subagent send <sessionId> "follow-up" --json
cursor-subagent watch <sessionId>
cursor-subagent close <sessionId> --json
cursor-subagent wave create --wave-id ... --goal ... --tasks tasks.json
cursor-subagent wave spawn <waveId> --json
cursor-subagent wave status|close <waveId>
Skill
.cursor/skills/cursor-subagent/SKILL.md
References for CLI, wave orchestration, and AGENTS.md snippet
Install & test
cd d:\code\automations
pip install -e ".[dev]"
export CURSOR_API_KEY="cursor_..."
python -m pytest -q   # 5 passed
Daemon is running at http://127.0.0.1:17340. To try a live session:

cursor-subagent spawn --task "Reply with the word ok" --cwd d:\code\automations --json
Copy the skill to ~/.cursor/skills/cursor-subagent/ for global use, or reference it from any project's AGENTS.md using the snippet in references/agent-md-snippet.md.